'use strict';

angular.module('controllers')
	.controller('ContactCtrl', ['$scope', '$http', '$routeParams', '$location', function ($scope, $http, $routeParams, $location) {
		$scope.init = function() {
		};
		$scope.init();
	}]);